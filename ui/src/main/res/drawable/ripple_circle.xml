<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@android:color/white">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners
                android:bottomLeftRadius="@dimen/loginButtonCornerRadius"
                android:bottomRightRadius="@dimen/loginButtonCornerRadius"
                android:topLeftRadius="@dimen/loginButtonCornerRadius"
                android:topRightRadius="@dimen/loginButtonCornerRadius" />
            <solid android:color="?android:colorAccent" />
        </shape>
    </item>
</ripple>