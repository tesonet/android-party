<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="editTextStyle">@style/EditAppTheme</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textViewStyle">@style/TextAppTheme</item>
        <item name="android:buttonStyle">@style/ButtonAppTheme</item>
        <item name="colorButtonNormal">@color/login_btn</item>
        <item name="android:textAppearanceButton">@style/ButtonAppTheme</item>
        <item name="actionBarSize">@dimen/minActionBarSize</item>
        <item name="android:icon">@drawable/ic_tesonet_logo</item>

        <!--Action bar style-->
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
    </style>

    <style name="AppTheme.ActionBar" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="android:height">@dimen/minActionBarSize</item>
        <item name="height">@dimen/minActionBarSize</item>
    </style>

    <style name="TextAppTheme" parent="android:Widget.TextView">
        <item name="android:textSize">24sp</item>
    </style>

    <style name="EditAppTheme" parent="Base.Widget.MaterialComponents.TextInputEditText">
        <item name="android:background">@color/white</item>
    </style>

    <style name="MyTheme" parent="android:Theme">

    </style>

    <style name="MyTextViewStyle" parent="android:Widget.TextView">
        <item name="android:textColor">#F00</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ButtonAppTheme" parent="@android:style/Widget.Button">
        <item name="android:background">@color/login_btn</item>
    </style>

    <dimen name="minActionBarSize">56dp</dimen>
</resources>
