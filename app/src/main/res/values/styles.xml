<resources>

    <style name="ToolBar" parent="Widget.AppCompat.ActionBar">
        <item name="elevation">0dp</item>
        <item name="background">@color/app_bar</item>
    </style>

    <style name="LoginLogo" parent="TextAppearance.Material3.DisplayLarge">
        <item name="android:textColor">@color/logo</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextInputLayout">
        <item name="boxStrokeColor">@color/text_input_box_stroke</item>
        <item name="hintTextColor">@color/input</item>
    </style>

    <style name="TextInputEditText">
        <item name="android:imeOptions">flagNoFullscreen</item>
        <item name="android:textCursorDrawable">@drawable/color_cursor</item>
    </style>

    <style name="MaterialButton">
        <item name="backgroundTint">@color/input</item>
    </style>

    <style name="HeaderTextView" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textStyle">bold</item>
        <item name="android:paddingStart">@dimen/side_margin</item>
        <item name="android:paddingEnd">@dimen/side_margin</item>
    </style>
</resources>