<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable name="handlers" type="app.androidparty.MainActivity"/>
        <variable name="isLoading" type="Boolean"/>
        <variable name="isHeaderVisible" type="Boolean"/>
        <import type="android.view.View" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/gradient_bg">

        <LinearLayout
            android:layout_width="match_parent"
            android:orientation="vertical"
            android:layout_height="match_parent">

            <RelativeLayout
                android:layout_width="match_parent"
                android:visibility="@{isHeaderVisible ? View.VISIBLE : View.GONE}"
                android:layout_margin="@dimen/header_margin"
                android:layout_height="wrap_content">

                <ImageView
                    android:layout_width="@dimen/header_logo"
                    android:layout_alignParentLeft="true"
                    android:src="@drawable/header_logo"
                    android:layout_height="wrap_content" />

                <ImageButton
                    android:layout_width="wrap_content"
                    android:id="@+id/logout"
                    android:clickable="true"
                    android:src="@drawable/ic_ico_logout"
                    android:onClick="@{() -> handlers.doLogOut()}"
                    android:layout_centerVertical="true"
                    android:background="@null"
                    android:layout_alignParentRight="true"
                    android:layout_height="wrap_content" />


            </RelativeLayout>

            <FrameLayout
                android:id="@+id/main_frame"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
            </FrameLayout>

        </LinearLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:visibility="@{isLoading ? View.VISIBLE : View.GONE}"
            android:background="@color/black_overlay"
            android:id="@+id/loaderContainer"
            android:layout_height="match_parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_centerInParent="true"
                android:orientation="vertical"
                android:layout_height="wrap_content">

                <ProgressBar
                    android:id="@+id/progressBar"
                    style="?android:attr/progressBarStyleLarge"
                    android:layout_width="@dimen/spinner_w"
                    android:layout_height="@dimen/spinner_h"
                    android:layout_gravity="center"
                    android:layout_marginBottom="@dimen/spinner_bottom_margin"
                    android:indeterminateDrawable="@drawable/spinner" >
                </ProgressBar>

                <TextView
                    android:textAlignment="center"
                    android:layout_width="match_parent"
                    android:textColor="#fff"
                    android:text="@string/fetching"
                    android:layout_height="wrap_content" />

            </LinearLayout>

        </RelativeLayout>

    </FrameLayout>

</layout>

